# boa_string_literal

This crate provides a single declarative macro, `js_str!`,
which converts a string literal into a const instance of `JsStr` of
the [`boa_string`](`https://crates.io/crates/boa_string`) crate.
